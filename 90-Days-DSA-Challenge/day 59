202. Happy Number

Topic - Hash Table, Math, Two Pointers

Code Implementation.
-----------------------------------------------------------------------
class Solution {
public:
    bool isHappy(int n) {  
        unordered_set<int> seen;
        while(n != 1){            
            if(seen.count(n)) 
                return false;
            seen.insert(n);
            int sum = 0;
            while(n > 0){
                int digit = n % 10; //number ka last digit
                sum += digit * digit; //number ka square deta hai 
                n /= 10; //last digit ko remove kar deta hai (0 banakar)
            }n = sum;
        }return true;
    }
};
dayday
